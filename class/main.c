#include <stdio.h>
#include <stdlib.h>

int main()
{
#include <stdio.h>  int main()
    {
        int totalRows = 5; //number of rows to display  // Row level Repetitive Action:  // Action1.Executes Column level Repetitive Action  // Action2.Move cursor to next row.  for( int row = 1; row <= totalRows; row ++) {  // Column level Repetitive Action:  for( int col = 1; col <= row; col++) {  // Action1.Move cursor in the same row  // Action2.print “*” character  printf("*");  }  printf("\n");// Move cursor to the next row  }  return 0;  }

Yakub, Mohmad. C Programming Patterns. Logical Thinking. Conquer Code Fear. The Definitive Guide To Learn To Code Quickly.:
        Programming Language fundamentals. Algorithmic Thinking. Beginners to Intermediate (p. 117). Kindle Edition.

    return 0;
}
